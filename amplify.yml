version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npx pnpm install --no-frozen-lockfile
    build:
      commands:
        - npx pnpm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
